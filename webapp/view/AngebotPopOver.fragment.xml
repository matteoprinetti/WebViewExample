<core:FragmentDefinition
	xmlns="sap.m"
	xmlns:core="sap.ui.core">
	<Popover
		id="angebotPopover"
		title="{Offers>OfrName}"
		class="sapUiResponsivePadding--header sapUiResponsivePadding--footer"
		placement="Bottom"
		initialFocus="email">
		<VBox>
 		<Text text="Start: { path: 'Offers>Startdat',   formatter: '.formatter.getDateAsWeek'}" class="sapUiSmallMargin"/>
 		<Text text="End: { path: 'Offers>Enddat',   formatter: '.formatter.getDateAsWeek'}" class="sapUiSmallMargin"/>
 		</VBox>
	</Popover>
</core:FragmentDefinition>
