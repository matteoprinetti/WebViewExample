_schema-version: "2.0.0"
ID: "zpolyplanung"
version: "1.0.0"
 
parameters:
  hcp-deployer-version: "1.0.0"

modules:
  - name: "zpolyplanung"
    type: html5
    path: .
    parameters:
      name: "zpolyplanung"
      version: 1.0.0-${timestamp}
    build-parameters:
      builder: grunt
      build-result: dist
